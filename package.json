{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "startApp": "lerna run startApp",
    "startServer": "lerna run startServer",
    "test": "lerna run test",
    "install": "npx lerna bootstrap --hoist",
    "migrate": "npx knex migrate:latest --knexfile app/backend/src/common/db/knexfile.ts"
  },
  "dependencies": {
    
  }
}
